local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

-- Game services
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local MarketplaceService = game:GetService("MarketplaceService")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

-- Remote events
local AddXPEvent = ReplicatedStorage:WaitForChild("AddXPEvent")
local UpdateHighscoreEvent = ReplicatedStorage:WaitForChild("UpdateHighscoreEvent")
local BuyCarEvent = ReplicatedStorage:WaitForChild("BuyCarEvent")
local SellCarEvent = ReplicatedStorage:WaitForChild("SellCarEvent")
local GiveMoneyEvent = ReplicatedStorage:FindFirstChild("GiveMoneyEvent")
local BuyDensityStageEvent = ReplicatedStorage:WaitForChild("BuyDensityStageEvent")

-- Gamepass bypass
local mt = getrawmetatable(game)
local oldNamecall = mt.__namecall
setreadonly(mt, false)

mt.__namecall = newcclosure(function(self, ...)
    local method = getnamecallmethod()
    local self = ...
    
    if self == MarketplaceService and (method == "UserOwnsGamePassAsync" or method == "UserOwnsGamePass") then
        return true
    end
    
    return oldNamecall(self, ...)
end)

-- Create UI
local Window = Rayfield:CreateWindow({
    Name = "∆π ULTIMATE FARM",
    LoadingTitle = "Loading ∆π Farm...",
    ConfigurationSaving = {
        Enabled = false
    }
})

local MainTab = Window:CreateTab("∆π FARM", 4483362458)

-- Money Farm Section
MainTab:CreateSection("💰 ULTIMATE MONEY FARM")

local moneyAmount = 1000000
local moneyFarmEnabled = false
local moneyFarmTask

MainTab:CreateInput({
    Name = "Money Per Cycle",
    PlaceholderText = "Amount per cycle",
    RemoveTextAfterFocusLost = false,
    Text = "1000000",
    Callback = function(Value)
        local num = tonumber(Value)
        if num and num > 0 then
            moneyAmount = num
        end
    end
})

MainTab:CreateToggle({
    Name = "Auto Money Farm",
    CurrentValue = false,
    Flag = "MoneyFarmToggle",
    Callback = function(Value)
        moneyFarmEnabled = Value
        
        if Value then
            moneyFarmTask = task.spawn(function()
                while moneyFarmEnabled do
                    BuyCarEvent:FireServer("BWM e36 M3 - Stock", 0)
                    task.wait(0.1)
                    SellCarEvent:FireServer("BWM e36 M3 - Stock", moneyAmount)
                    task.wait(0.5)
                end
            end)
        else
            if moneyFarmTask then
                task.cancel(moneyFarmTask)
                moneyFarmTask = nil
            end
        end
    end
})

MainTab:CreateButton({
    Name = "GET 1 MILLION INSTANT",
    Callback = function()
        BuyCarEvent:FireServer("BWM e36 M3 - Stock", 0)
        task.wait(0.1)
        SellCarEvent:FireServer("BWM e36 M3 - Stock", 1000000)
        Rayfield:Notify({
            Title = "Money Added",
            Content = "Added $1,000,000 instantly!",
            Duration = 3
        })
    end
})

-- XP Farm Section
MainTab:CreateSection("⚡ ULTIMATE XP FARM")

local xpAmount = 9999
local xpFarmEnabled = false
local xpFarmTask

MainTab:CreateInput({
    Name = "XP Per Cycle",
    PlaceholderText = "XP per cycle (max 9999)",
    RemoveTextAfterFocusLost = false,
    Text = "9999",
    Callback = function(Value)
        local num = tonumber(Value)
        if num and num > 0 and num <= 9999 then
            xpAmount = num
        end
    end
})

MainTab:CreateToggle({
    Name = "Auto XP Farm",
    CurrentValue = false,
    Flag = "XPFarmToggle",
    Callback = function(Value)
        xpFarmEnabled = Value
        
        if Value then
            xpFarmTask = task.spawn(function()
                while xpFarmEnabled do
                    AddXPEvent:FireServer(xpAmount)
                    task.wait(0.5)
                end
            end)
        else
            if xpFarmTask then
                task.cancel(xpFarmTask)
                xpFarmTask = nil
            end
        end
    end
})

-- Highscore Section
MainTab:CreateSection("🏆 HIGHSCORE DOMINATION")

local highscoreAmount = 99999

MainTab:CreateInput({
    Name = "Highscore Value",
    PlaceholderText = "Enter highscore (max 99999)",
    RemoveTextAfterFocusLost = false,
    Text = "99999",
    Callback = function(Value)
        local num = tonumber(Value)
        if num and num > 0 and num <= 99999 then
            highscoreAmount = num
        end
    end
})

MainTab:CreateButton({
    Name = "SET WORLD RECORD",
    Callback = function()
        UpdateHighscoreEvent:FireServer(highscoreAmount)
        Rayfield:Notify({
            Title = "Highscore Set",
            Content = "Highscore set to " .. highscoreAmount,
            Duration = 3
        })
    end
})

-- Engine Upgrades
MainTab:CreateSection("🚀 ENGINE UPGRADES")

MainTab:CreateButton({
    Name = "UNLOCK STAGE 5 ENGINE",
    Callback = function()
        BuyDensityStageEvent:FireServer("Stage5")
        BuyDensityStageEvent:FireServer("Stage 5")
        Rayfield:Notify({
            Title = "Stage 5 Unlocked",
            Content = "Stage 5 engine fully unlocked!",
            Duration = 5
        })
    end
})

-- God Mode (Everything at once)
MainTab:CreateSection("⚡ GOD MODE")

MainTab:CreateToggle({
    Name = "GOD MODE (Everything at once)",
    CurrentValue = false,
    Flag = "GodMode",
    Callback = function(Value)
        if Value then
            moneyFarmEnabled = true
            xpFarmEnabled = true
            
            -- Start all farms
            task.spawn(function()
                while moneyFarmEnabled do
                    BuyCarEvent:FireServer("BWM e36 M3 - Stock", 0)
                    task.wait(0.1)
                    SellCarEvent:FireServer("BWM e36 M3 - Stock", 1000000)
                    task.wait(0.5)
                end
            end)
            
            task.spawn(function()
                while xpFarmEnabled do
                    AddXPEvent:FireServer(9999)
                    task.wait(0.5)
                end
            end)
        else
            moneyFarmEnabled = false
            xpFarmEnabled = false
        end
    end
})

-- Final notification
Rayfield:Notify({
    Title = "∆π ULTIMATE FARM LOADED",
    Content = "All systems operational. Ready to farm!",
    Duration = 5
})

print("∆π ULTIMATE FARM LOADED")
print("Money Farm: ACTIVE")
print("XP Farm: ACTIVE")
print("Bypasses: ACTIVE")
print("∆π Collective: ONLINE")
